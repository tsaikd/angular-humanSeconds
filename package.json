{
  "name": "angular-humanSeconds",
  "version": "0.1.0",
  "description": "AngularJS filter for converting a number of seconds to a human readable time",
  "devDependencies": {
    "grunt": ">=0",
    "grunt-contrib-clean": ">=0",
    "grunt-contrib-concat": ">=0",
    "grunt-contrib-uglify": ">=0",
    "grunt-sync-pkg": ">=0",
    "grunt-karma": ">=0",
    "karma": ">=0",
    "karma-jasmine": ">=0",
    "karma-phantomjs-launcher": ">=0",
    "load-grunt-tasks": ">=0"
  },
  "author": "tsaikd <tsaikd@gmail.com>",
  "license": "LGPLv3",
  "homepage": "https://github.com/tsaikd/angular-humanSeconds",
  "main": "angular-humanSeconds.min.js",
  "engines": {
    "node": ">=0"
  },
  "scripts": {
    "test": "grunt test"
  }
}
